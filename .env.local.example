# Public site URL used for sitemap, robots, Open Graph, and schema
NEXT_PUBLIC_SITE_URL=https://ashokstudiophotography.com

# Password for /login (server-side). Prefer ADMIN_PASSWORD in Vercel.
ADMIN_PASSWORD=ashokstudio2026

# Optional fallback for older local setups
NEXT_PUBLIC_ADMIN_PASSWORD=ashokstudio2026

# Created automatically when you connect a Vercel Blob store to this project
# Vercel → Project → Storage → Create Blob → Connect to project
# BLOB_READ_WRITE_TOKEN=
